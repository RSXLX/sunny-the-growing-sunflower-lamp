# 预览图说明

`本版/`：当前交付项目的 `workbench-room.png` 和 `hardware-assembly.png`，与原项目 `evidence/` 内对应文件一致。

六页工作台、手机布局等更多图片在 `../01_MVP项目/growing-lamp-mvp/evidence/` 中。

`此前版本参考/`：对话中此前版本的桌面与移动端图片，原文件名为 `workbench-desktop.png` 和 `workbench-mobile.png`。仅作视觉历史参考，不保证与当前源码逐项一致，不作为本版测试证据。

所有硬件图片均应按结构预览理解，不能当作实物制造或光学实拍证明。
